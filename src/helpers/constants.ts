export const DEFAULT_PER_PAGE = 12
export const DEFAULT_ITEM_WIDTH = 240
export const DEFAULT_WIDTH = 720
export const DEFAULT_HEIGHT = 405

export const INITIAL_WIDTH = 100

export const OVERLAY_TAG_NAME = 'vbx-overlay'
export const INLINE_TAG_NAME = 'vbx-inline'

export enum PlayerType {
    Overlay = 'videobox',
    Inline = 'vbinline'
}

export enum PlayerEventType {
    OpenChange = 'open-change'
}
